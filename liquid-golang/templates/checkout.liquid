<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Inter:wght@400;600&display=swap');
        h1, h2, h3 { font-family: 'Playfair Display', serif; }
        body { font-family: 'Inter', sans-serif; }
    </style>
</head>
<body class="bg-stone-50 text-stone-900">
    <!-- Navigation -->
    <nav class="bg-white border-b border-stone-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-20 items-center">
                <a href="/" class="text-2xl font-bold text-amber-800 tracking-tighter">{{ site_name }}</a>
                <a href="/menu" class="text-sm font-semibold uppercase tracking-widest hover:text-amber-700 transition">&larr; Back to Menu</a>
            </div>
        </div>
    </nav>

    <main class="py-24">
        <div class="max-w-3xl mx-auto px-4">
            <h1 class="text-4xl mb-12 text-center">Complete Your Order</h1>

            <div class="bg-white rounded-3xl shadow-xl overflow-hidden mb-8">
                <div class="p-8 border-b border-stone-100">
                    <h2 class="text-xl font-bold mb-6">Order Summary</h2>
                    <div class="space-y-4">
                        {% for item in cart_items %}
                        <div class="flex justify-between items-center">
                            <div>
                                <h4 class="font-bold text-stone-800">{{ item.name }}</h4>
                                <p class="text-xs text-stone-400 uppercase tracking-widest">{{ item.variant }}</p>
                            </div>
                            <div class="text-right">
                                <p class="font-bold">${{ item.price }}</p>
                                <p class="text-xs text-stone-400">Qty: {{ item.qty }}</p>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                </div>
                
                <div class="p-8 bg-stone-50">
                    <div class="flex justify-between items-center text-xl font-bold">
                        <span>Total Amount</span>
                        <span class="text-amber-800">${{ total }}</span>
                    </div>
                </div>
            </div>

            <!-- Payment Demo Form -->
            <div class="bg-white rounded-3xl shadow-xl p-8">
                <h2 class="text-xl font-bold mb-8">Payment Details</h2>
                <form class="space-y-6">
                    <div>
                        <label class="block text-xs font-bold uppercase tracking-widest text-stone-400 mb-2">Cardholder Name</label>
                        <input type="text" placeholder="John Doe" class="w-full px-4 py-3 rounded-xl border border-stone-200 focus:border-amber-600 focus:ring-0 transition">
                    </div>
                    <div>
                        <label class="block text-xs font-bold uppercase tracking-widest text-stone-400 mb-2">Card Number</label>
                        <input type="text" placeholder="**** **** **** ****" class="w-full px-4 py-3 rounded-xl border border-stone-200 focus:border-amber-600 focus:ring-0 transition">
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-bold uppercase tracking-widest text-stone-400 mb-2">Expiry Date</label>
                            <input type="text" placeholder="MM/YY" class="w-full px-4 py-3 rounded-xl border border-stone-200 focus:border-amber-600 focus:ring-0 transition">
                        </div>
                        <div>
                            <label class="block text-xs font-bold uppercase tracking-widest text-stone-400 mb-2">CVV</label>
                            <input type="text" placeholder="***" class="w-full px-4 py-3 rounded-xl border border-stone-200 focus:border-amber-600 focus:ring-0 transition">
                        </div>
                    </div>
                    <button type="button" class="w-full bg-amber-800 text-white py-5 rounded-2xl font-bold text-lg hover:bg-amber-900 transition shadow-lg shadow-amber-800/20">
                        Pay ${{ total }} Now
                    </button>
                </form>
                <p class="text-center text-xs text-stone-400 mt-6 flex items-center justify-center">
                    <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20"><path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path></svg>
                    Secure SSL Encrypted Payment
                </p>
            </div>
        </div>
    </main>

    <footer class="bg-stone-900 text-stone-400 py-16">
        <div class="max-w-7xl mx-auto px-4 text-center">
            <p class="text-sm">&copy; {{ current_year }} {{ site_name }}. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>

